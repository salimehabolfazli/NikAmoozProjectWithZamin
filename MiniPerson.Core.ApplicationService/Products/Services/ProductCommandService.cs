namespace WebLog.Core.ApplicationService.Products.Services
{
    public class ProductCommandService
    {
        //public async Task CheckCategoryActivateAsync(IProductCategoryCommandRepository productCategoryCommandRepository, Product product)
        //{
        //    var category = await productCategoryCommandRepository.GetAsync(product.CategoryId);
        //    if (category.IsActive == false)
        //        throw new InvalidEntityStateException(ProductResource.ProductCategoryNotActivateError);

        //    product.Active();
        //}
    }
}
